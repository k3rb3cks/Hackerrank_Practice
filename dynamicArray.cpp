#include<bits/stdc++.h>

int main()
{
    std::vector<std::vector<int>> queries(5);

    for(int i = 0; i < 3 ; i++)
    {
        queries[i].resize(2);

    }

    std::cout<<queries.size();

    std::vector<int> arr(4);

    std::cout<<arr.at(3);
}